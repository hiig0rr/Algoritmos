#include <stdio.h>
int main() {
    int num1, num2, soma;
    printf("Digite dois números inteiros:\n");
    scanf("%d %d", &num1, &num2);
    soma = num1 + num2;
     printf("A soma dos dois números é: %d\n", soma);

    return 0;
}